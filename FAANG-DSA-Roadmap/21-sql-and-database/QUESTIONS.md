# SQL and Database

## SQL Basics

- [ ] Combine Two Tables
  - Problem statement: Given the database tables, write the SQL query for Combine Two Tables and return exactly the requested rows and columns.
- [ ] [IMP] Second Highest Salary
  - Problem statement: Given the database tables, write the SQL query for Second Highest Salary and return exactly the requested rows and columns.
- [ ] [IMP] Employees Earning More Than Their Managers
  - Problem statement: Given the database tables, write the SQL query for Employees Earning More Than Their Managers and return exactly the requested rows and columns.
- [ ] [IMP] Duplicate Emails
  - Problem statement: Given the database tables, write the SQL query for Duplicate Emails and return exactly the requested rows and columns.
- [ ] Customers Who Never Order
  - Problem statement: Given the database tables, write the SQL query for Customers Who Never Order and return exactly the requested rows and columns.
- [ ] Delete Duplicate Emails
  - Problem statement: Given the database tables, write the SQL query for Delete Duplicate Emails and return exactly the requested rows and columns.
- [ ] Rising Temperature
  - Problem statement: Given the database tables, write the SQL query for Rising Temperature and return exactly the requested rows and columns.
- [ ] Classes More Than 5 Students
  - Problem statement: Given the database tables, write the SQL query for Classes More Than 5 Students and return exactly the requested rows and columns.
- [ ] Not Boring Movies
  - Problem statement: Given the database tables, write the SQL query for Not Boring Movies and return exactly the requested rows and columns.
- [ ] Swap Salary
  - Problem statement: Given the database tables, write the SQL query for Swap Salary and return exactly the requested rows and columns.
- [ ] Big Countries
  - Problem statement: Given the database tables, write the SQL query for Big Countries and return exactly the requested rows and columns.
- [ ] Recyclable and Low Fat Products
  - Problem statement: Given the database tables, write the SQL query for Recyclable and Low Fat Products and return exactly the requested rows and columns.
- [ ] Find Customer Referee
  - Problem statement: Given the database tables, write the SQL query for Find Customer Referee and return exactly the requested rows and columns.
- [ ] Customer Placing the Largest Number of Orders
  - Problem statement: Given the database tables, write the SQL query for Customer Placing the Largest Number of Orders and return exactly the requested rows and columns.
- [ ] Article Views I
  - Problem statement: Given the database tables, write the SQL query for Article Views I and return exactly the requested rows and columns.

## Joins and Aggregation

- [ ] [IMP] Department Highest Salary
  - Problem statement: Given the database tables, write the SQL query for Department Highest Salary and return exactly the requested rows and columns.
- [ ] [IMP] Department Top Three Salaries
  - Problem statement: Given the database tables, write the SQL query for Department Top Three Salaries and return exactly the requested rows and columns.
- [ ] [IMP] Trips and Users
  - Problem statement: Given the database tables, write the SQL query for Trips and Users and return exactly the requested rows and columns.
- [ ] Game Play Analysis I
  - Problem statement: Given the database tables, write the SQL query for Game Play Analysis I and return exactly the requested rows and columns.
- [ ] Game Play Analysis II
  - Problem statement: Given the database tables, write the SQL query for Game Play Analysis II and return exactly the requested rows and columns.
- [ ] Game Play Analysis III
  - Problem statement: Given the database tables, write the SQL query for Game Play Analysis III and return exactly the requested rows and columns.
- [ ] [IMP] Game Play Analysis IV
  - Problem statement: Given the database tables, write the SQL query for Game Play Analysis IV and return exactly the requested rows and columns.
- [ ] [IMP] Managers with at Least 5 Direct Reports
  - Problem statement: Given the database tables, write the SQL query for Managers with at Least 5 Direct Reports and return exactly the requested rows and columns.
- [ ] [IMP] Confirmation Rate
  - Problem statement: Given the database tables, write the SQL query for Confirmation Rate and return exactly the requested rows and columns.
- [ ] Monthly Transactions I
  - Problem statement: Given the database tables, write the SQL query for Monthly Transactions I and return exactly the requested rows and columns.
- [ ] Immediate Food Delivery II
  - Problem statement: Given the database tables, write the SQL query for Immediate Food Delivery II and return exactly the requested rows and columns.
- [ ] Product Sales Analysis I
  - Problem statement: Given the database tables, write the SQL query for Product Sales Analysis I and return exactly the requested rows and columns.
- [ ] Product Sales Analysis II
  - Problem statement: Given the database tables, write the SQL query for Product Sales Analysis II and return exactly the requested rows and columns.
- [ ] Project Employees I
  - Problem statement: Given the database tables, write the SQL query for Project Employees I and return exactly the requested rows and columns.
- [ ] Project Employees II
  - Problem statement: Given the database tables, write the SQL query for Project Employees II and return exactly the requested rows and columns.
- [ ] Sales Analysis III
  - Problem statement: Given the database tables, write the SQL query for Sales Analysis III and return exactly the requested rows and columns.
- [ ] Restaurant Growth
  - Problem statement: Given the database tables, write the SQL query for Restaurant Growth and return exactly the requested rows and columns.
- [ ] Friend Requests II: Who Has the Most Friends
  - Problem statement: Given the database tables, write the SQL query for Friend Requests II: Who Has the Most Friends and return exactly the requested rows and columns.
- [ ] Investments in 2016
  - Problem statement: Given the database tables, write the SQL query for Investments in 2016 and return exactly the requested rows and columns.
- [ ] Tree Node
  - Problem statement: Given the database tables, write the SQL query for Tree Node and return exactly the requested rows and columns.

## Window Functions

- [ ] [IMP] Rank Scores
  - Problem statement: Given the database tables, write the SQL query for Rank Scores and return exactly the requested rows and columns.
- [ ] [IMP] Consecutive Numbers
  - Problem statement: Given the database tables, write the SQL query for Consecutive Numbers and return exactly the requested rows and columns.
- [ ] [IMP] Human Traffic of Stadium
  - Problem statement: Given the database tables, write the SQL query for Human Traffic of Stadium and return exactly the requested rows and columns.
- [ ] Exchange Seats
  - Problem statement: Given the database tables, write the SQL query for Exchange Seats and return exactly the requested rows and columns.
- [ ] [IMP] Customers Who Bought All Products
  - Problem statement: Given the database tables, write the SQL query for Customers Who Bought All Products and return exactly the requested rows and columns.
- [ ] Market Analysis I
  - Problem statement: Given the database tables, write the SQL query for Market Analysis I and return exactly the requested rows and columns.
- [ ] Market Analysis II
  - Problem statement: Given the database tables, write the SQL query for Market Analysis II and return exactly the requested rows and columns.
- [ ] Last Person to Fit in the Bus
  - Problem statement: Given the database tables, write the SQL query for Last Person to Fit in the Bus and return exactly the requested rows and columns.
- [ ] Find Median Given Frequency of Numbers
  - Problem statement: Given the database tables, write the SQL query for Find Median Given Frequency of Numbers and return exactly the requested rows and columns.
- [ ] Movie Rating
  - Problem statement: Given the database tables, write the SQL query for Movie Rating and return exactly the requested rows and columns.
- [ ] Sales Person
  - Problem statement: Given the database tables, write the SQL query for Sales Person and return exactly the requested rows and columns.
- [ ] User Activity for the Past 30 Days I
  - Problem statement: Given the database tables, write the SQL query for User Activity for the Past 30 Days I and return exactly the requested rows and columns.
- [ ] User Activity for the Past 30 Days II
  - Problem statement: Given the database tables, write the SQL query for User Activity for the Past 30 Days II and return exactly the requested rows and columns.
- [ ] Active Businesses
  - Problem statement: Given the database tables, write the SQL query for Active Businesses and return exactly the requested rows and columns.
- [ ] The Number of Seniors and Juniors to Join the Company
  - Problem statement: Given the database tables, write the SQL query for The Number of Seniors and Juniors to Join the Company and return exactly the requested rows and columns.

## Database Concepts

- [ ] Explain primary key, foreign key, candidate key, and composite key.
  - Problem statement: Given the database tables, write the SQL query for Explain primary key, foreign key, candidate key, and composite key. and return exactly the requested rows and columns.
- [ ] Explain normalization and denormalization.
  - Problem statement: Given the database tables, write the SQL query for Explain normalization and denormalization. and return exactly the requested rows and columns.
- [ ] [IMP] Explain indexes and B-tree indexes.
  - Problem statement: Given the database tables, write the SQL query for Explain indexes and B-tree indexes. and return exactly the requested rows and columns.
- [ ] Explain clustered vs non-clustered indexes.
  - Problem statement: Given the database tables, write the SQL query for Explain clustered vs non-clustered indexes. and return exactly the requested rows and columns.
- [ ] [IMP] Explain transactions and ACID.
  - Problem statement: Given the database tables, write the SQL query for Explain transactions and ACID. and return exactly the requested rows and columns.
- [ ] [IMP] Explain isolation levels.
  - Problem statement: Given the database tables, write the SQL query for Explain isolation levels. and return exactly the requested rows and columns.
- [ ] Explain deadlocks and how to prevent them.
  - Problem statement: Given the database tables, write the SQL query for Explain deadlocks and how to prevent them. and return exactly the requested rows and columns.
- [ ] [IMP] Explain query plans.
  - Problem statement: Given the database tables, write the SQL query for Explain query plans. and return exactly the requested rows and columns.
- [ ] Explain sharding and partitioning.
  - Problem statement: Given the database tables, write the SQL query for Explain sharding and partitioning. and return exactly the requested rows and columns.
- [ ] Explain replication and consistency.
  - Problem statement: Given the database tables, write the SQL query for Explain replication and consistency. and return exactly the requested rows and columns.

